services:
  mysql:
    image: 'mysql:latest'
    ports:
      - '3306'
    environment:
      - 'MYSQL_DATABASE=database'
      - 'MYSQL_PASSWORD=password'
      - 'MYSQL_ROOT_PASSWORD=password'
      - 'MYSQL_USER=moyeohaeng'
